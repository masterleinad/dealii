sudo : false

notifications:
  email: false

language: C++

compiler: 
    - gcc

cache: 
  directories:
    - programs

addons:
  apt:
    packages:
      - doxygen

before_install:
    - sudo apt-get -y install graphviz perl texlive-binaries

install:
    - ./contrib/utilities/download_clang_format

script: 
#    - ./contrib/utilities/check_indentation.sh 
    - mkdir build
    - cd build
    - cmake -DDEAL_II_COMPONENT_DOCUMENTATION=ON ..
    - make documentation
    - cat doxygen.log && ! [ -s doxygen.log ]

branches:
  only:
    - master
